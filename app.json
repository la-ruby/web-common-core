{
  "environments": {
    "test": {
      "scripts": {
        "test": "yarn install && bundle install && bundle exec rails test",
        "postdeploy": "bundle exec rake db:seed"
      },
      "addons":[
        "heroku-postgresql"
      ]
    }
  }
}
